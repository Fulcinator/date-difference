/**
 * import whatever is needed to call and check your function
 */

export function main(){

    console.log("Write here your code")
    //modules: process.env.BABEL_ENV === 'esm' ? false : 'auto' ,
    //{ modules: process.env.BABEL_ENV === 'esm' ? false : 'auto' },
}

main();
